const divider = (a, b) => {
	return a / b;
};

const multiplier = (a, b) => {
	return a * b;
};

const adder = (a, b) => {
	return a + b;
};

const subber = (a, b) => {
	return a - b;
};

export { adder, divider, multiplier, subber };
